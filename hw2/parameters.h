#ifndef PARAMETERS_H
#define PARAMETERS_H

const int N = 1000; // NxN space
const int M = 100000; // M rounds

const int gridNum = 10;
const int blockNum = 10;

#define T(x, y) ((x) + (N+2)*(y))

#endif